services:
  - type: web
    name: my-discord-bot
    env: node
    plan: free
    region: frankfurt
    branch: main
    buildCommand: "npm install"
    startCommand: "node index.js"
    autoDeploy: true
    healthCheckPath: /
